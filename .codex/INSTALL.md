# install discord-selfbot-mcp

1. run `npx discord-selfbot-mcp-setup`
2. choose automatic browser extraction or manual token entry
3. select the correct client to auto-configure (claude, cursor, or opencode)
4. confirm the configuration is saved
5. tell user to restart their client
